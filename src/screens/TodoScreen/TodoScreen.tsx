import React, { FC } from "react";

import styles from './TodoScreen.module.css';

interface IProps { };

const TodoScreen: FC<IProps> = (props) => {
  return (
    <div>TodoScreen</div>
  )
};

export default TodoScreen;